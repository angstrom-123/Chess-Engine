{
  "scripts": {
    "dev": "tsc && fastapi dev",
    "start": "tsc && fastapi run",
    "format": "prettier --write ./client/src",
    "reinstall": "make -C build && uv pip install -e . --force-reinstall"
  },

  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
